name: Ping Render

on:
  schedule:
    - cron: '*/2 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Send ping to Render
        run: curl -X GET https://seu-site-no-render.com
